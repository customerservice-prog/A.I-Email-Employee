/** @enum {string} */
const TRACK = {
  AUTO: 'auto',
  REVIEW: 'review',
};

/** @enum {string} */
const STATUS = {
  PENDING: 'pending',
  SENT: 'sent',
  ESCALATED: 'escalated',
  FAILED: 'failed',
};

module.exports = { TRACK, STATUS };
